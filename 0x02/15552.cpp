#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int a;
    cin >> a;
    while (a--) {
        int x, y;
        cin >> x >> y;
        cout << x+y << "\n";
    }
}