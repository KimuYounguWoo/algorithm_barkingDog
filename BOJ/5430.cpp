#include <bits/stdc++.h>
using namespace std;

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int N;
    cin >> N;
    while (N--) {
        string str;
        cin >> str;
        int num;
        cin >> num;
        deque<int> D;
        for (int i=0; i<num; i++) D.push_front(i);

        for (auto c : str) {

        }
    }
}